<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- Sensor hostname is user-configurable and LAN sensors serve plain HTTP,
		     so a per-domain exception is not possible. Do not add narrower keys
		     (e.g. NSAllowsLocalNetworking) alongside this one: their presence makes
		     iOS 10+ ignore NSAllowsArbitraryLoads entirely. -->
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
</dict>
</plist>
